{
  "routes": [
    {
      "method": "GET",
      "path": "/ventas",
      "handler": "ventas.find",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/ventas/count",
      "handler": "ventas.count",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/ventas/:id",
      "handler": "ventas.findOne",
      "config": {
        "policies": []
      }
    },
    {
      "method": "POST",
      "path": "/ventas",
      "handler": "ventas.create",
      "config": {
        "policies": []
      }
    },
    {
      "method": "PUT",
      "path": "/ventas/:id",
      "handler": "ventas.update",
      "config": {
        "policies": []
      }
    },
    {
      "method": "DELETE",
      "path": "/ventas/:id",
      "handler": "ventas.delete",
      "config": {
        "policies": []
      }
    }
  ]
}
